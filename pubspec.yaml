name: task_sync
description: "A smart schedule reminder application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  http: ^1.2.0
  intl: ^0.19.0
  provider: ^6.1.1
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0
  shared_preferences: ^2.5.5
  mask_text_input_formatter: ^2.9.0
  android_alarm_manager_plus: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - public/
